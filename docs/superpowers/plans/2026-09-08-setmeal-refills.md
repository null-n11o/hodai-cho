# 定食おかわり自由の追加計画

設計: ../specs/2026-09-08-setmeal-refills-design.md

## Task 1: カタログと検索ジャンルを追加

- [x] `定食おかわり自由` の型、日英表示、料理イラストを追加。
- [x] 公式確認済み6店を別データファイルに追加し、蔵部は訪問情報を注記。
- [x] ジャンル絞り込み、カタログ検証、画像マッピングのテストを追加・通過。
- [x] 全テストと本番ビルドを実行する。

### 検証

`tests/okawari.test.ts` 2件、`npm test`、`npm run build` を通す。SSGでは新店の個別ページとエリアページが生成されることを確認する。
