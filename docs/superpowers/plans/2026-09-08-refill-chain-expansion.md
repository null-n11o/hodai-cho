# 定食おかわり自由チェーン拡張の実装計画

- [x] 公式のサービス案内・店舗ページを調査し、対象チェーンと代表店舗を確定する。
- [x] 既存のカタログテストをチェーン掲載条件へ拡張する（RED）。
- [x] `okawari-chains.ts` を追加し、東京・神奈川の店舗、コース、日英注記、公式リンクを実装する（GREEN）。
- [x] 駅・チェーンの英語名テーブルとカタログバージョンを更新する。
- [x] `npm test`、`npm run lint`、`npm run build:ssg` を実行して引き渡す。

検証対象は、定食おかわり自由の検索フィルタ、未知ID表示など既存画面を含む全テストと本番ビルドとする。
