# 初回入口LP

2026-09-12。ユーザーが添付したLPを選択し、メインコピー「今日は好きなだけ食べよう。」と写真上の「ジャンルイメージ」削除を指定。選択と変更指示を設計承認として扱う。

## 目的と導線

トップ `/` と `/en/` をサービス紹介LPにする。既存検索は `/search/` と `/en/search/` に移し、主CTA「食べ放題を探す」と副CTA「条件を指定して探す」から入る。初回判定や強制リダイレクト、追加の永続化は導入しない。ロゴはトップ、探すナビ・空状態・戻る・パンくずは検索へ。詳細・保存・エリア・問い合わせの既存URLと機能を維持する。

## 視覚とコピー

添付 `tabeho-lp.png` と修正版 `exec-98591822-d701-49c6-b9d4-d23ded497f9c.png` が視覚ターゲット。ダークヘッダーのロゴ＋日本語/EN、地域ラベル＋食卓の新聞、太い3行見出し「今日は／好きなだけ／食べよう。」、説明、料理写真、大きい主CTA、罫線で分けた01〜03の説明、副CTA、免責フッター。

説明: 東京・神奈川の食べ放題のお店を、料金・制限時間・最寄り駅などの条件で探せるグルメ情報サイトです。
3項目: 予算で絞る／時間で選ぶ／駅から探す。本文は添付の内容に合わせる。
英語見出し: Today, eat to your heart’s content. 英語CTA: Find all-you-can-eat。説明と3項目と免責も英訳する。

## Global Constraints

- UIは日英対応（既定は日本語、`/en/` 以下に英語、`hreflang` 付き）。文書言語もパスに同期。
- page #f3efe8 / surface #fbfaf7 / text #29231e / muted #756b61 / line #d5cbbf / accent #c4543a / shell #171512。見出し・本文・番号はNoto Sans JP。
- ユーザーのPC・スマホ対応指示により、LPは900px以上で最大幅1180px、コピーと写真の2列構成・特徴3列。900px未満は最大幅680pxの縦構成で中央寄せ。既存検索等の幅は変更しない。ページ横スクロール禁止。44px以上の操作面、可視フォーカス、150–400msの動き、prefers-reduced-motionで無効。
- 写真は既存 `/food/hero-all-day.png`、ロゴは既存 `/brand/tabeho-logo-header.png`。LP写真は装飾画像としてalt=""、ユーザー指定で「ジャンルイメージ」は表示しない。既存店カードの画像説明は変更しない。
- ネオン・紫・金・絵文字・人物写真・外部口コミ星・ダミー店・実績捏造は禁止。
- 店舗はCatalogRepository経由、保存・料金表示・予約リンク・公式リンクの既存仕様を維持。店舗追加なし。
- LPにも共通の料金・条件免責と日英アフィリエイト広告表示を出す。画像説明のみLPで省略可能なSiteDisclaimer拡張を使う。
- マージ・デプロイはしない。既存未マージの作業ブランチを基底とする差分PRを作成。

## 実装範囲

LandingPageと専用CSS、共有searchPath関数、トップ時のみのヘッダー表示、全検索導線の付け替え、SSGトップと検索のメタ/canonical/hreflang/sitemap、関連テスト。検索ロジック・カタログは変更しない。

## 検証

TDDでLPの日英コピー・両CTA遷移・検索の直接表示・検索への戻り・言語切替・SSRを確認。全テスト、lint、build:ssg。ブラウザで320/390/1280px、日英、検索・詳細・保存を確認し、添付とレイアウト比較を記録する。
